{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"{
            "version": 2,
            "builds": [{ "src": "package.json", "use": "@vercel/node" }],
            "routes": [
              { "src": "/cosmos", "dest": "/Swap.fixture.tsx" }
            ]
          }     
      }
    ]
}